name: Deploy Spring Boot to Pi

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Prepare Jar
        run: |
          mkdir -p target/deploy
          mv target/*.jar target/deploy/spring-app.jar

      - name: Install Cloudflare Client
        run: |
          sudo mkdir -p --mode=0755 /usr/local/bin
          curl -L --output /usr/local/bin/cloudflared https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          sudo chmod +x /usr/local/bin/cloudflared

      - name: Deploy to Pi via Tunnel
        env:
          # Biến này lấy từ chuỗi Base64 bạn đã dán vào GitHub Secrets
          SSH_KEY_BASE64: ${{ secrets.PI_SSH_KEY }}
          PI_USER: ${{ secrets.PI_USERNAME }}
          PI_HOST: ssh.minhthien.io.vn
        run: |
          # 1. Thiết lập thư mục SSH và giải mã Key từ Base64
          mkdir -p ~/.ssh
          echo "$SSH_KEY_BASE64" | base64 -d > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          
          # 2. Tạo file cấu hình SSH (Dùng IdentityFile là id_ed25519)
          cat <<EOF > ~/.ssh/config
          Host my-pi
            HostName $PI_HOST
            User $PI_USER
            IdentityFile ~/.ssh/id_ed25519
            ProxyCommand /usr/local/bin/cloudflared access ssh --hostname %h
            StrictHostKeyChecking no
          EOF

          # 3. Copy file JAR sang Pi
          scp target/deploy/spring-app.jar my-pi:/home/minhthien/app/spring-app.jar

          # 4. Restart App bằng PM2
          ssh my-pi "cd /home/minhthien/app && \
          if [ -f .env ]; then export \$(grep -v '^#' .env | xargs); fi && \
          pm2 delete spring-app || true && \
          pm2 start 'java -Xmx768M -jar spring-app.jar' \
          --name 'spring-app' \
          -- \
          --spring.datasource.url=\$SPRING_DATASOURCE_URL \
          --spring.datasource.username=\$SPRING_DATASOURCE_USERNAME \
          --spring.datasource.password=\$SPRING_DATASOURCE_PASSWORD \
          --app.jwt.secret=\$APP_JWT_SECRET \
          --cloudinary.cloud-name=\$CLOUDINARY_CLOUD_NAME \
          --cloudinary.api-key=\$CLOUDINARY_API_KEY \
          --cloudinary.api-secret=\$CLOUDINARY_API_SECRET \
          --spring.mail.username=\$SPRING_MAIL_USERNAME \
          --spring.mail.password=\$SPRING_MAIL_PASSWORD \
          --google.client-id=\$GOOGLE_CLIENT_ID \
          --facebook.app-id=\$FACEBOOK_APP_ID \
          --facebook.app-secret=\$FACEBOOK_APP_SECRET \
          --payos.client-id=\$PAYOS_CLIENT_ID \
          --payos.api-key=\$PAYOS_API_KEY \
          --payos.checksum-key=\$PAYOS_CHECKSUM_KEY \
          --app.public.base-url=\$APP_PUBLIC_BASE_URL \
          --server.forward-headers-strategy=framework && \
          pm2 save"